<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- .Bridge 可自定义按钮颜色 -->
    <style name="Theme.LearnAndroidJava" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!--        <item name="android:statusBarColor">@android:color/transparent</item>-->
        <!-- Customize your theme here. -->
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.LearnAndroidJava" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!--  启动屏 splash  -->
    <style name="Theme.Splash" parent="Theme.LearnAndroidJava">
        <item name="android:windowBackground">@drawable/splash</item>
        <!--    启动页全屏    -->
        <item name="android:windowFullscreen">true</item>
        <!--    导航条透明   -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.Tool" parent="android:Theme.Material.Light.NoActionBar" />
</resources>